<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Brokey_APP.ViewModels"
             x:Class="Brokey_APP.Views.LoginPage"
             x:DataType="vm:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource BrokeyBackground}">

    <ScrollView>
        <Grid RowDefinitions="Auto,*" Padding="0">

            <!-- Top area with Brokey mascot -->
            <Grid Grid.Row="0" HeightRequest="300" BackgroundColor="{StaticResource Secondary}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brokey Mascot -->
                <Image Grid.Row="0"
                       Source="brokey_mascot.png"
                       HeightRequest="160"
                       Aspect="AspectFit"
                       Margin="0,40,0,0"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"/>

                <!-- Speech Bubble -->
                <Border Grid.Row="1"
                        BackgroundColor="{StaticResource White}"
                        StrokeShape="RoundRectangle 20"
                        Stroke="Transparent"
                        Padding="20,12"
                        Margin="40,0,40,20"
                        HorizontalOptions="Center">
                    <Border.Shadow>
                        <Shadow Brush="{StaticResource Gray300Brush}" Offset="0,2" Radius="8" Opacity="0.2"/>
                    </Border.Shadow>
                    <Label Text="Welcome back! I missed you! 😊"
                           Style="{StaticResource SpeechBubbleText}"/>
                </Border>
            </Grid>

            <!-- Login Form -->
            <VerticalStackLayout Grid.Row="1" Padding="30,30,30,20" Spacing="16">

                <!-- Brokey Logo Text -->
                <Label Text="Brokey"
                       Style="{StaticResource BrokeyLogoText}"
                       Margin="0,0,0,5"/>

                <!-- Error Message -->
                <Border BackgroundColor="#FFF0F0"
                        StrokeShape="RoundRectangle 12"
                        Stroke="#FFD0D0"
                        Padding="12,10"
                        IsVisible="{Binding HasError}">
                    <Label Text="{Binding ErrorMessage}"
                           TextColor="#D32F2F"
                           FontFamily="PoppinsRegular"
                           FontSize="13"
                           HorizontalTextAlignment="Center"/>
                </Border>

                <!-- Email -->
                <Border BackgroundColor="{StaticResource White}"
                        StrokeShape="RoundRectangle 15"
                        Stroke="{StaticResource Gray200}"
                        Padding="16,4"
                        HeightRequest="55">
                    <Entry Placeholder="Email"
                           Text="{Binding Email}"
                           Keyboard="Email"
                           ReturnType="Next"
                           FontFamily="PoppinsRegular"/>
                </Border>

                <!-- Password -->
                <Border BackgroundColor="{StaticResource White}"
                        StrokeShape="RoundRectangle 15"
                        Stroke="{StaticResource Gray200}"
                        Padding="16,4"
                        HeightRequest="55">
                    <Entry Placeholder="Password"
                           Text="{Binding Password}"
                           IsPassword="True"
                           ReturnType="Done"
                           FontFamily="PoppinsRegular"/>
                </Border>

                <!-- Forgot Password -->
                <Button Text="Forgot Password?"
                        Style="{StaticResource LinkButton}"
                        Command="{Binding ForgotPasswordCommand}"
                        HorizontalOptions="End"
                        Margin="0,-8,0,0"/>

                <!-- Login Button -->
                <Button Text="Login"
                        Command="{Binding LoginCommand}"
                        HorizontalOptions="Fill"
                        Margin="0,5,0,0"/>

                <!-- Go to Register -->
                <HorizontalStackLayout HorizontalOptions="Center" Spacing="0" Margin="0,10,0,0">
                    <Label Text="No account yet? "
                           TextColor="{StaticResource Gray500}"
                           FontFamily="PoppinsRegular"
                           FontSize="14"
                           VerticalOptions="Center"/>
                    <Button Text="Sign Up"
                            Style="{StaticResource LinkButton}"
                            TextColor="{StaticResource BrokeyOrange}"
                            FontFamily="PoppinsSemiBold"
                            Command="{Binding GoToRegisterCommand}"/>
                </HorizontalStackLayout>

                <!-- Loading -->
                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>
    </ScrollView>
</ContentPage>

